use crate::recipe::Ingredient;

mod item;
mod recipe;

fn main() -> Result<(), String> {
    // let rec = recipe::SteelBeam;

    // let mut q: Vec<Box<dyn recipe::Recipe>> = vec![Box::new(rec)];

    // while let Some(rec) = q.pop() {
    //     for Ingredient {qtty: _, item} in rec.get_ingredients() {
    //         println!("{item:?}");

    //     }
    //     println!("{rec:?}");
    //     println!("{q:?}")
    // }

    // let println!("{:#?}", recipe.get_ingredients());
    Ok(())
}
